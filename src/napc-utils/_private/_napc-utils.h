#if !defined(PV_NAPC_UTILS_h)
	#define PV_NAPC_UTILS_h

	#include <napc-utils/napc-utils.h>

	#include <string.h> // memset(), strlen(), strcmp(), strcasecmp(), strncpy(), memcmp()
	#include <stdio.h> // vsnprintf()
#endif
